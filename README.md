# AI-Powered Task Scheduler

This project applies classical AI search algorithms like DFS, BFS, Topological Sort, and A* to intelligently manage task execution based on dependencies and priorities. Tasks are modeled as nodes in a directed graph with real-time visualizations and RESTful API support. Built with Node.js and a JavaScript frontend, it enables efficient and responsive scheduling across dynamic workflows.

